<nav class="navbar navbar-expand-lg navbar-light ws-primary">
    <div class="container-fluid">
        <img src="{{ asset('build/images/logo.png') }}" alt=""/>
        <a class="navbar-brand" href="{{ path('app_index') }}">Wild Series</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Link</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Category
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('category_show', { 'categoryName' : 'Action'}) }}">Action</a></li>
                        <li><a class="dropdown-item" href="{{ path('category_show', { 'categoryName' : 'Aventure'}) }}">Aventure</a></li>
                        <li><a class="dropdown-item" href="{{ path('category_show', { 'categoryName' : 'Animation'}) }}">Animation</a></li>
                        <li><a class="dropdown-item" href="{{ path('category_show', { 'categoryName' : 'Fantastique'}) }}">Fantastique</a></li>
                        <li><a class="dropdown-item" href="{{ path('category_show', { 'categoryName' : 'Horreur'}) }}">Horreur</a></li>

                    </ul>
                </li>
            </ul>
            <form class="d-flex">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
        </div>
        {% if app.user %}
            {{ app.user.email }}
            <a href="{{ path('app_logout') }}"> Se déconnecter</a>
        {% else %}
            <a href="{{ path('app_login') }}"> Se connecter</a>
        {% endif %}

    </div>
</nav>
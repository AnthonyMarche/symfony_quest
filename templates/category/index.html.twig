{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}


{% block body %}
    <ul>
        {% for category in categories %}
            <li><a href="{{ path("category_show", { categoryName: category.name }) }}">{{ category.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}